#include <iostream>
//#include <string>
using namespace std;
/*4. namespace my {
	int value = 10;
}

namespace your {
	int value = 20;
}
*/
int main(int argc, char const* argv[])
{
	// 1. cout << "Hello World!!" << endl;
	/* 2.int first = 0, second = 0;
	cin >> first>>second;
	cout << first << ", " << second << endl;*/
	/*3
	//char buf[255] = "Hello World!";
	char buf[255] = {};
	// 다음 줄에 키보드로 부터 Hello World! 를 입력받아 buf 에 저장하는 코드 작성
	cin.getline(buf, 15, '\n'); //공백이 포함된 문자열을 입력받고 출력할때는 getline을 써야한다.
	cout << buf << endl; */
	/*4.cout << my::value << endl;
	cout << your::value << endl;*/
	/*5.cout << "Hello" << endl; //또는 cout <<"Hello"\n
	cout << "World";*/
	/*6.int a = 20;
	cout << a;*/
	/*7. int value = 65;  // 'A' 의 ASCII 값은 65 임 
	cout << value << endl;
	int ch = value;
	// value에 저장된 값을 변수 ch를 선언하고 value의 값을 ch에 복사하는 코드를 작성
	//다른 방법은 char ch=value;로 정의해서 cout<<ch<<endl;로 정수를 문자열로 변환후 출력된다.

	cout.put(ch); //ch 값에 해당하는 문자를 cout.put(정수)로 출력한다.*/
	/*8. cout << "true" << endl; //문자열 true이다
	cout << true << endl;// 부울형으로 맞는 값이라고 하므로 1을 출력한다.
	cout << "false" << endl;//문자열 false이다
	cout << false << endl;//부울형으로 틀린 값이라고 하므로 0을 출력한다.*/

	return 0;
}